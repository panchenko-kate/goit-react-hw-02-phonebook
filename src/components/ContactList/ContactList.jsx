import { nanoid } from 'nanoid';
const id = nanoid();

export const ContactList = ({ contacts }) => {
    return (
            <ul>
            {/* {contacts.map(contact => ( */}
            {/* <li id={id}>{contact.name} */}
            <li>
            </li>
            {/* ))} */}
            </ul>
    )
};